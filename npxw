#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LOCAL_NODE_BIN="$ROOT_DIR/.tools/node-v22.22.2-darwin-arm64/bin"

if [[ -d "$LOCAL_NODE_BIN" ]]; then
  PATH="$LOCAL_NODE_BIN:$PATH"
fi

exec npx "$@"
